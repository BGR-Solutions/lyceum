package com.lyceum.academic.domain.enums;

/**
 * Enum representing the type of notification.
 * This enum is used to categorize notifications into different types, such as email, audit, and report notifications.
 * 
 * An sample enum to show more possibilities of the project updates.
 */
public enum NotificationType {
    EMAIL,
    AUDIT,
    REPORT
}
