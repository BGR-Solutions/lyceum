package com.lyceum.academic.domain.enums;

/**
 * Enum representing the status of an enrollment.
 * This enum is used to track the current state of an enrollment, such as pending, confirmed, or cancelled.
 */
public enum EnrollmentStatus {
    PENDING,
    CONFIRMED,
    CANCELLED
}
