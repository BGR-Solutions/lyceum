package com.lyceum.academic.domain.enums;

/**
 * Enum representing the type of a course.
 * This enum is used to categorize courses into different types, such as undergraduate, postgraduate, and extension courses.
 * 
 * An sample enum to show more possibilities of the project updates.
 */
public enum CourseType {
    UNDERGRADUATE,
    POSTGRADUATE,
    EXTENSION
}
