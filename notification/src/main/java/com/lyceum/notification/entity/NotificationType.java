package com.lyceum.notification.entity;

/**
 * Enum representing the types of notifications that can be sent.
 * This enum is used to specify the method of notification delivery, such as email, push notification, or SMS.
 */
public enum NotificationType {
    INFO,
    EMAIL,
    PUSH,
    SMS
}
