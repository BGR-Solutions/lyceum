# ── MSG DOCKER PROFILE ───────────────────────────────────────────────────
# Usado ao subir a solução via Docker Compose.
# O host é o nome do serviço RabbitMQ declarado no docker-compose.yml.
# Credenciais podem ser sobrescritas por variáveis de ambiente.

spring:
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest
